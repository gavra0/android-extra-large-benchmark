default-scenarios = ["androidStudioSync"]

androidStudioSync {
    title = "Android Studio Sync"
    versions = [ "7.5.1" ]
    warm-ups = 1
    iterations = 1
    android-studio-sync {
        # Override default Android Studio jvm args
        studio-jvm-args = ["-Xms256m", "-Xmx10g"]
    }
}